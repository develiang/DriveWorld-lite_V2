seed: 42
precision: bf16
micro_batch_size: 1
gradient_accumulation_steps: 1
num_workers: 0
learning_rate: 0.0001
weight_decay: 0.01
warmup_steps: 1
max_steps: 2
checkpoint_every: 2
validate_every: 1
validation_batches: 1
log_every: 1
gradient_clip_norm: 1.0
gradient_checkpointing: true
ema_decay: 0.99
output_dir: artifacts/runs/smoke-gpu-4x8
